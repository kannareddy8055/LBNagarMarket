{
  "name": "veg-ledger",
  "version": "1.0.0",
  "description": "Root workspace for Mandi ERP",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "start": "node backend/index.js",
    "install:all": "npm install --legacy-peer-deps --prefix frontend && npm install --prefix backend",
    "build": "npm run install:all && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
